# Installed inert. An operator enables the plugin with a local patch that
# supplies one exact assignment; that patch lives outside this repository.
- insert:
    - id: aizign-workflow-signal
      name: "@aizign/adapter-dsh"
      disabled: true
