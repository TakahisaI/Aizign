[package]
name = "aizign-core"
description = "Pure, deterministic decisions for Aizign software-change workflows"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish.workspace = true
exclude = ["AGENTS.md"]

# `aizign-core` has no dependencies of any kind. Adding one requires an ADR
# and an update to docs/architecture/dependency-rules.md (checked by
# `cargo xtask public-audit`).
[dependencies]

[dev-dependencies]

[lints]
workspace = true
