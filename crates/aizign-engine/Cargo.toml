[package]
name = "aizign-engine"
description = "Aizign application engine: use cases, ports, and effect claims around the core"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish.workspace = true
exclude = ["AGENTS.md"]

[dependencies]
aizign-core.workspace = true

[dev-dependencies]
aizign-testkit.workspace = true

[lints]
workspace = true
